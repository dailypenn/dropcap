{
  "name": "dropcap",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node bin/www",
    "deploy": "gcloud beta functions deploy dropcap --stage-bucket cloud-function-deployments --trigger-http"
  },
  "dependencies": {
    "cors": "^2.8.4",
    "express": "^4.13.3",
    "googleapis": "^22.2.0",
    "open-graph-scraper": "^3.5.1"
  },
  "devDependencies": {
    "eslint": "^5.4.0"
  }
}
