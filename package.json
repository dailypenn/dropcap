{
  "name": "dropcap",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node bin/www",
    "deploy": "gcloud functions deploy dropcap --stage-bucket cloud-function-deployments --trigger-http --runtime nodejs12"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "googleapis": "^39.1.0",
    "open-graph-scraper": "^3.6.1"
  },
  "devDependencies": {
    "eslint": "^5.16.0"
  }
}
